# Supabase Configuration
# Required for persisting leads.
# SUPABASE_URL: Your Supabase project URL (e.g., https://xyz.supabase.co)
SUPABASE_URL=

# SUPABASE_SERVICE_ROLE_KEY: The "service_role" secret key.
# CRITICAL: This is a SERVER-ONLY secret. Do NOT expose this to the client (browser).
# It allows bypassing Row Level Security to insert leads reliably.
SUPABASE_SERVICE_ROLE_KEY=

# Site URL
# Used for canonical URLs, Open Graph images, sitemap generation, and API origin checks.
# In local dev: http://localhost:3000
# In production: https://your-domain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Analytics (Optional)
# Set to 'true' to enable the analytics script (e.g., Plausible).
NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Analytics Domain (Optional)
# If using Plausible, defaults to betterlife.example.com
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Environment Flag (Optional)
# Can be 'local', 'preview', or 'production'
NEXT_PUBLIC_ENV=local
